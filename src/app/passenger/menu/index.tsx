'use client';

import { FC } from 'react';

const PassengerMenu: FC = () => {
  return (
    <div>
      <h1>Menú Principal</h1>
      <p>Accedé a todas las funciones del pasajero TyDy desde aquí.</p>
    </div>
  );
};

export default PassengerMenu;
